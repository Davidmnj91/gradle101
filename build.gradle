allprojects {
    group 'com.example.multi-module-gradle'
    version '1.0-SNAPSHOT'
}

subprojects {

    // common plugins
    apply plugin: 'java'

    // common repositories
    repositories {
        mavenCentral()
        mavenLocal()
    }

}
